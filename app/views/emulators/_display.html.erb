<%= render partial: "shared/toolbar", locals: { text: "Emulator for output '#{@emulator.output.name}' trained with length scale #{number_with_precision(@emulator.length_scale)}, process variance #{number_with_precision(@emulator.process_variance)} and normalisation #{(@emulator.normalisation ? "enabled" : "disabled")}.", parent: @project, object: @emulator, export_options: ["matlab", "json"] } %>